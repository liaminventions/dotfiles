export GTK_THEME=Adwaita:dark
export PATH=~/.npm-global/bin:$PATH
#eval `ssh-agent -s`

export WINEESYNC=1

[[ -f ~/.bashrc ]] && . ~/.bashrc

if [ -z "$WAYLAND_DISPLAY" ] && [ "$XDG_VTNR" -eq 1 ]; then
  exec sway
fi
