CACHE_DIR="~/.cache/eww"
if ! [ -f "$CACHE_DIR/chem.lock" ]; then
  FILE=/home/waverider/Pictures/Screenshots/chem/"$(date +%Y%m%d-%H%M%S)".png
elif ! [ -f "$CACHE_DIR/math.lock" ]; then
  FILE=/home/waverider/Pictures/Screenshots/math/"$(date +%Y%m%d-%H%M%S)".png
else
  FILE=/home/waverider/Pictures/Screenshots/"$(date +%Y%m%d-%H%M%S)".png
fi
#grim -g "$(slurp)" $FILE && wl-copy < $FILE
grimblast --freeze copysave area $FILE
